
/*
public class SHOP : MonoBehaviour
{
    public LevelManager levelManager;

    public GameObject hero1IMG;
    
    public GameObject shopMenuPanel;

    public ShopPanel shopPanel;
    public ItemsManager itemManager;
   // public float KnockbackWeknessHERO2;

    



    void Start()
    {
        levelManager = FindObjectOfType<LevelManager>();
        shopPanel = FindObjectOfType<ShopPanel>();
        itemManager = FindObjectOfType<ItemsManager>();
       
        
    }


    public void OnTriggerStay2D(Collider2D collision)
    {
        if (collision.tag == "PLAYER" )
        {

            if (Input.GetKeyDown(KeyCode.Return))
            {
                
                /*
                levelManager.coinsPlayerPanel1.text = "COINS: " + levelManager.coinCounter;
                levelManager.coinsPlayerPanel2.text = "COINS: " + levelManager.coinCounter;
                levelManager.coinsPlayerPanel3.text = "COINS: " + levelManager.coinCounter;
                levelManager.starsPlayerPanelInne.text = "STARS: " + levelManager.starCounter;
              //  levelManager.starsPlayerPanelWin.text = "STARS: " + levelManager.starCounter;*/

              /*  shopPanel.StatsCostRefresh();

                shopPanel.HERO1StatsValuesRefresh();
                shopPanel.HERO2StatsValuesRefresh();
                shopPanel.HERO3StatsValuesRefresh();
                shopPanel.HERO4StatsValuesRefresh();
                shopPanel.GeneralStatsValuesRefresh();

                shopMenuPanel.SetActive(true);
                Time.timeScale = 0.0f;
                // hero1IMG.GetComponent<Animator>().speed = hero1IMG.GetComponent<Animator>().speed * Time.fixedDeltaTime;

                levelManager.LvlPanel_NawArrow.SetActive(false);

               

            }


        }
    }

    
}
*/